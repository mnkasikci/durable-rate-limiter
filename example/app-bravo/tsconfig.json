{
  // Extends the root config verify-style; imports from `dist/`, typecheck only.
  // Its own project so its global `Env` never merges with app-alpha's — see the
  // note in ../app-alpha/tsconfig.json.
  // Run with: npm run example:typecheck  (after npm run build)
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "types": ["@cloudflare/workers-types"],
    "noEmit": true,
    "declaration": false,
    "emitDeclarationOnly": false
  },
  "include": ["src", "../shared"]
}
